---
name: video-white-model-prompt
description: 当用户明确要求把已上传的视频转换为视频白模、深度视频、单目相对深度视频或 depth-map video 时使用。可按用户选择仅生成白模，或并行生成白模和 Qwen3.8-Max 反推提示词。不要因仅提示词反推、普通视频分析、视频复刻、产品替换、静态图片深度或 3D 白模需求而触发。
metadata:
  version: 1.0.0
---

# 视频白模与提示词反推

把唯一参考视频转换为近白远黑的单目相对深度白模；用户选择时，同时用 Qwen3.8-Max 理解视频画面、原始音轨及可选产品图，输出与白模逐段对应的生视频提示词。

## 触发边界

仅在用户明确要求处理“视频”并生成白模视频、深度视频、单目深度序列或 depth-map video 时触发。“白模”语义不清或可能指 3D/建筑白模时先澄清。

用户只要求提示词反推、视频分析、复刻或产品替换时不触发。触发后若用户明确只要白模，尊重其范围，不询问产品信息或 Qwen Key，也不调用外部 API。

## 用户交互

### 1. 确认唯一视频

只把当前消息明确上传或提到的视频视为候选，不扫描下载目录猜测。没有视频时请用户上传；有多个候选时列出文件名让用户选择；唯一视频可直接进入参数收集。原视频始终只读。

### 2. 一次性收集参数

必须收集：

- 产出范围：`仅白模` 或 `白模+提示词`。
- 最大分段时长：`15s` 或 `30s`。

仅当选择“白模+提示词”时继续收集：

- 产品名称，可选；没有时记为未提供，不编造名称或卖点。
- 产品图片，可选，最多 9 张；默认是同一产品的不同视角或细节。明显包含多个产品时先确认产品与图片映射。
- API Key 来源：已配置的 `DASHSCOPE_API_KEY`，或用户提供的 Key 文件/目录绝对路径。目录路径默认解析其中的 `DASHSCOPE_API_KEY.md`。不要要求用户在聊天中粘贴 Key。

已经在当前任务中明确提供的信息不要重复询问。

### 3. 只读预检

确认前检查：视频可读、常见视频格式、帧率与时长、是否含音轨、产品图可读、Key 来源可用、`ffmpeg`/`ffprobe`、Python 依赖、深度模型和临时磁盘空间。当前白模管线拒绝 VFR；发现 VFR 时说明原因并停止，不擅自转码。

深度模型按以下顺序解析：命令参数、`DEPTH_ANYTHING_MODEL`、Skill 本地 `models/depth_anything_v2_vits.onnx`、当前机器的已知模型路径。均不存在时请用户提供模型路径，不自动下载。

### 4. 唯一确认点

汇总视频、规格、产出范围、15/30 秒、产品信息、Key 来源和调试模式。选择“白模+提示词”时明确说明：视频、原始音轨和产品图会发送至阿里云 Qwen API，可能产生费用。用户明确回复确认后才能运行。

用户确认后冻结本次输入；处理中新增的文件默认属于下一次运行，除非用户明确取消当前任务。

## 执行

为每次运行创建新的用户可见输出目录，不复用旧目录。调用统一脚本：

```bash
python3 <skill-root>/scripts/run_pipeline.py \
  --video <参考视频绝对路径> \
  --scope depth-and-prompt \
  --segment-max-seconds <15或30> \
  --product-name <可选产品名称> \
  --product-image <可选图片，可重复最多9次> \
  --api-key-file <可选Key文件或目录> \
  --output-dir <新的输出目录>
```

仅白模时使用 `--scope depth-only`，省略产品和 Key 参数。用户明确要求校验完整 API 数据时增加 `--save-debug`；默认不保存包含媒体 Base64 的完整请求体。

“白模+提示词”模式会并行启动 Qwen 请求和全片深度推理。Qwen 输出每段时长与 `segment_plan.json`；深度推理完成后按其中的累计切分点编码白模，保证白模段与提示词段顺序对应。最后一段因真实帧时长可比提示词整数时长短不到 1 秒，这是已接受的误差。

白模必须保持：统一模型与预处理、全片统一深度归一化、近白远黑、适度时间稳定、原画幅比例、原帧顺序、CFR 帧率、H.264、720p、MP4、无音频、每段不超过用户选择的上限。

运行时持续给出真实进度，不虚构百分比。Qwen 只对明确可重试的 HTTP 状态有限重试；未知超时不自动重发，避免重复计费。一条链路失败时保留另一条已成功产物，只重试失败链路。

## 交付

两条链路达到终态后统一交付：

- 按顺序展示全部白模 MP4，并提供可点击文件链接。
- 提供完整 `prompt.txt` 和 `segment_plan.json`。
- 每一段提示词必须完整放入独立的 `text` 代码块，便于复制；不能只发文件路径，也不能用“其余略”截断。
- 用户要求调试数据时再提供请求体和响应体 JSON；提醒其中包含源媒体 Base64，但不包含 API Key。
- 简述实际分段时长、产品名称、产品图数量、编码规格和失败项。

不要创建额外总结 Markdown。`prompts/video_reverse_system_prompt.txt` 是发给 Qwen 的运行时数据，不是本 Skill 对 Codex 的操作指令。
